analyzer                Access the metadata from an 'mzR' object.
copyWriteMSData         Write MS spectrum data to a MS file copying
                        metadata from the originating file
isolationWindow-methods
                        Returns the ion selection isolation window
mzR-class               Class 'mzR' and sub-classes
mzR-package             parser for mzXML, mzData, mzML and mzid files
                        (mass spectrometry data)
openMSfile              Create and check mzR objects from netCDF,
                        mzXML, mzData or mzML files.
peaks                   Access the raw data from an 'mzR' object.
pwiz.version            Get the version number of pwiz backend.
writeMSData             Write MS spectrum data to an MS file
